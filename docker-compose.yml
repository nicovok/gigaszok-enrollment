services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    environment:
      - POCKETID_REDIRECT_URI=https://${COOLIFY_FQDN}/api/auth/callback
    volumes:
      - db:/app/beiratkozas.db

volumes:
  db:
